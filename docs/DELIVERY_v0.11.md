# 智析录 v0.11.0 终版交付说明

基线：v0.10.0（GitHub）改造，未重写。本文逐条对照《删除清单》与《补全清单》。

## 一、删除清单（逐条核销）

| 项 | 状态 | 落地 |
|----|------|------|
| 成长页整页（环/身份/趋势/NextStep）及路由与入口 | ✅ | features/growth、core/growth、growth_providers 全删；/growth 路由早已移除，无入口 |
| 毕业制 | ✅ | 复习页毕业区/毕业卡/掌握度5分流删除 |
| 7 天预测图 | ✅ | forecast 计算与 UI 删除 |
| 遗忘曲线可视化 | ✅ | ForgettingCurveMini 组件删除（推荐卡/详情页引用清零） |
| 记忆状态卡 | ✅ | 详情页 _MemoryStateCard + _Stat 删除 |
| 统计独立页 | ✅ | stats_page.dart 删除（/stats 路由 v15 已删），孤儿链 AiReviewPlanner/AiPathAdvisor/KnowledgePointStat/ai_learning_services.dart 一并删除 |
| 解析任务队列 | ✅ | v0.8.0 已删，本轮核查无残留 |
| MOSS/专注/监控/锁屏/Mission 残留 | ✅ | 全仓 grep 清零（仅余迁移历史注释） |
| 导航三 Tab+中央相机键 | ✅ | 错题本｜📷｜复习｜设置；GlassNavBar 自适应布局（并修复其硬编码越界导致的白屏） |

连带删除的孤儿代码：energy_ring / forgetting_curve / growth_illustration 组件（EmptyState 为通用组件已单独保留）、growth_metrics + learning_events 两张表（schema v5 DROP）。

## 二、补全清单（逐条核销）

### 3.1 拍
- 应用内相机（camera 包，非系统 intent）✅
- 底部「单页|多页」切换 ✅（持久化）
- 多页=拍一张→进裁剪→确认返回继续拍 ✅（本轮重做：编辑屏 returnCamera 模式，确认携带裁剪结果返回；缩略图堆叠+数量；队列可删+长按拖动重排）
- 相册导入唯一入口 ✅
- 裁剪/旋转/四角/使用原图 ✅（android.graphics 管线 + Kotlin 侧 Matrix.setPolyToPoly；OpenCV 仅可选自动检测，未加载时自动校准提示"不可用，已保留手动"）
- 增强默认开可切回原图，原图保留 ✅
- 失败说人话 ✅（裁剪几何纯函数化：越界钳制+退化拦截，页内提示）

### 3.2 存
- 科目必选 ✅
- 知识点级联：学科→册→章→节→知识点，人教 2023 真实册名（必修/选择性必修，无八九年级）✅ + 搜索框 ✅ + 每级自定义兜底 ✅ + 历史值置顶 ✅（版本级移出 UI 固定人教版；节级数据集未收录时自动省略）
- AI 打标签仅配置后可用，失败落手动不阻塞 ✅
- 题干/答案选填、图片即题干 ✅

### 3.3 库
- 科目 Tab 数量角标 + 掌握/时间筛选 ✅
- 卡片=缩略图+旗标+日期 ✅
- 详情=大图点按缩放+标签面包屑+题干答案+真实编辑/删除（溢出菜单）✅

### 3.4 复习
- SM-2 排期 ✅（EF 1.3-3.0 双侧 clamp + 间隔 365 天封顶，修复溢出 bug）
- 理由标签（第 N 次/逾期 N 天）✅
- 会话与推荐卡大图展示图片，无"(图片题，未填写题干)"占位 ✅
- 三档标记常驻，下次日期变化可见（toast 前后对比）✅
- 未填答案隐藏"显示答案"、提示直接自评 ✅
- 每日提醒通知开关 ✅（本轮新增：权限前置说明对话框 → 系统权限 → 每日定时；改时重排；开机恢复接收器）

### 3.5 导出
- 多选→PDF：嵌中文字体（霞鹜文屏 Lite）、增强图、答案开关、答案位置（题后/统一最后）、字号标准/大号、保存/分享/系统打印、白纸预览 ✅

### 3.6 备份
- 坚果云（地址写死+应用密码提示）/InfiniCLOUD（地址自动拼）/自定义 WebDAV/本地导出 ✅
- 全量备份含 AI 配置（name/baseUrl/model）✅
- API Key 仅设备份密码时加密入包（ai_keys.json 在 AES 包内，恢复自动写回）；未设密码不入包且恢复后引导重输 ✅
- 恢复后题目/图片/复习状态/设置一致 ✅（VACUUM INTO 快照 + 图片集）
- 自动（dirty+退后台+WiFi 策略）+手动+上次结果可见 ✅（本轮新增）

### 3.7 设置
- AI 服务商（持久化/Key 走 secure_storage/测试连接显状态码/模型 sheet 搜索+智能排序+视觉标签）✅
- 云备份 ✅；云同步（Supabase 单账号自动）✅（本轮新增）
- 外观三态，深色全页对比度≥4.5:1 ✅（gray5 深色 3.8:1 不达标→自适应 muted 色；卡片/底栏玻璃色深色适配）
- 关于 ✅

### 3.8 AI 举一反三
- 解析放宽→格式强化重试一次→仍失败 AI 原文作练习卡标"AI 拟题" ✅
- 每题来源标签 ✅；未配置仅本地同类题检索+引导 ✅

## 三、全局质量铁律
- Toast：提示 2.2s / 错误 4s 带「知道了」可点消 / 路由观察器清跨页残留 / 2s 去重 ✅
- UI 零开发者词汇：字符串字面量扫描清零（宁浅勿造等内部文案仅余于代码注释）✅
- EmptyState 整体居中 ✅；系统字体缩放自适应（弹性布局）✅；Material 默认灰件替换为玻璃拟物 ✅；关键操作触感（快门/评分 HapticFeedback）✅

## 四、自查证据
1. 裁剪几何单测（13 条，含四边形拖拽回归：连续越界拖拽仍合法/极限拖拽/共线退化拦截）✅ CI 可复跑
2. 无 OpenCV 时四动作可用路径：手动四角拖拽→cropByPoints（android.graphics 透视拉正）；旋转（Matrix）；裁剪同上；使用原图（纯文件操作）——均不经过 OpenCV；自动校准按钮显示"不可用，已保留手动"
3. PDF 无乱码：字体嵌入单测（豆腐块清零）✅ + 待你真机截图确认
4. 深色四页截图：待你真机拍摄（本环境无 Android 设备）
5. 备份→恢复一致性：本地无设备验证，待你真机执行（备份→卸载→重装→恢复→核对题目/图片/复习日期）
6. AI 全失败路径：断网（AiCall 超时分级→页内提示不阻塞）+ 错误 Key（HTTP 401 真实状态码展示）——待你真机演示

## 五、Supabase 单账号说明（按你的要求）
- 全 App 只有一个账号（zhiilu_sync@outlook.com），安装即自动登录，无需任何操作
- 数据隔离：所有表 RLS 按 user_id；存储桶按用户目录
- 完整性约束：updated_at 冲突仲裁、删除墓碑（本地删题→云端级联删）、孤儿图片清理、全表 try/catch 单表失败不拖累整体
- 长久使用：Supabase 免费档对个人数据量足够；如未来停用，数据可随时用 WebDAV/本地导出兜底

## 六、未做完/需声明项
1. PDF 截图、深色四页截图、备份恢复对照、AI 失败路径演示——本环境无 Android 设备，全部需你真机完成（APK 见 Release）
2. 知识库"节"级：内置数据集物理到章级（节=知识点），其余学科到册级；节级 UI 已实现，数据集未收录的层级自动省略+自定义兜底（不编造教材内容）
3. 通知为每日固定文案（含到期数），不做精确到分钟的实时推送
